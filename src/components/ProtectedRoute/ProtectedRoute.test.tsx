import { render, screen, cleanup } from "@testing-library/react";
import ProtectedRoute from "./index";

test.todo("should render ProtectedRoute component");
test.todo("should render outlet if component have access token");
test.todo("should render login component if don't have access token");
